@model DomainModel.ViewModels.Settings.CustomerTypeAddEditModel
@using GolpaMotorFinal.Models.ViewModels.CRUD
@{
    var form = new CrudFormViewModel
    {
        Controller = "Settings",
        Action = "Edit",
        GridId = "CustomerTypeGrid",
        RefreshGridUrl = "List",
        Enctype = "application/x-www-form-urlencoded",
        SubmitButtonText = "ویرایش"
    };
}

<crud-form model="form">
    @Html.AntiForgeryToken()
    <input asp-for="CustomerTypeID" type="hidden" />
    <div asp-validation-summary="ModelOnly" class="text-danger mb-2"></div>
    <div class="row g-3">
        <div class="col-12">
            <label asp-for="Title" class="form-label"></label>
            <input asp-for="Title" class="form-control" />
            <span asp-validation-for="Title" class="text-danger small"></span>
        </div>
    </div>
    <div class="form-actions">
        <button type="button" class="btn btn-secondary cancel-modal">انصراف</button>
        <button type="submit" class="main_bt">ویرایش</button>
    </div>
</crud-form>
